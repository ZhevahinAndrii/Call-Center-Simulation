# 📞 Call Center Simulation | Симулятор Кол-центру

## 📝 Огляд Проекту

Цей проект є інтерактивним симулятором, розробленим для моделювання роботи кол-центру. що дозволяє аналізувати навантаження контакт-центру та порівнювати ефективність операторів і чат-ботів. Далі ми коротко представимо технології, методологію, архітектуру та результати.»

Інтерфейс розроблений за допомогою **Streamlit**, що забезпечує легку взаємодію та візуалізацію результатів.

---

## 🚀 Технологічний Стек

* **Основний додаток:** Python 3.11
* **Фреймворк:** Streamlit
* **Контейнеризація:** Docker, Docker Compose
* **Конфігурація:** Змінні середовища (`.env`)

---

## 🛠️ Початок Роботи (Запуск через Docker Compose)

Для запуску проекту в ізольованому середовищі використовуйте Docker Compose. Цей метод гарантує правильний мапінг портів і роботу Streamlit на потрібному внутрішньому порту, вказаному в змінних оточення

### 1. Передумови

* Встановлений [Docker](https://www.docker.com/get-started) (включаючи Docker Compose).
* Встановлений `git`.

### 2. Клонування репозиторію та налаштування

Клонуйте проект та перейдіть у кореневу директорію:

```bash
git clone https://github.com/ZhevahinAndrii/Call-Center-Simulation.git
cd Call-Center-Simulation
```

### 3. Конфігурація Портів(.env)
Створіть у корені проекту файл з назвою .env. Значення, вказані нижче, є стандартними для цього проекту і гарантують доступ до симулятора через порт 8080.

```dotenv
# === 1. ЗОВНІШНЯ КОНФІГУРАЦІЯ (для доступу з браузера) ===
# Порт, який буде прокинуто назовні Docker або використано локально
APP_PORT=8080
# Адреса для відображення в інтерфейсі
APP_HOST=localhost

# === 2. ВНУТРІШНЯ КОНФІГУРАЦІЯ STREAMLIT (для контейнера) ===
# Хост, на якому Streamlit слухає (0.0.0.0 обов'язково для Docker)
STREAMLIT_SERVER_ADDRESS=0.0.0.0
# Порт, на якому Streamlit запускається всередині контейнера
STREAMLIT_SERVER_PORT=8502
```

### 4. Збірка та Запуск(Локально)
Рекомендовано, якщо вносите зміни у код

Виконайте наступні команди. **Зверніть увагу:** оскільки ваш `docker-compose.yml` має `build: .` і ви використовуєте змінні з `.env`, ви завжди маєте починати зі збірки.

```bash
# Крок 1: Збірка образу (застосовує зміни в Dockerfile)
docker compose build

# Крок 2: Запуск контейнера у фоновому режимі
docker compose up -d
```

### 💻 Використання Додатку
Після успішного запуску контейнера, симулятор буде доступний у вашому браузері.
- URL: `http://localhost:8080`

#### Зупинка та очищення
Щоб зупинити та видалити контейнер і мережу:
```bash
docker compose down
```

### 📂 Структура Проекту

```
.
├── app/
│   └── streamlit_app.py   # Головний файл інтерфейсу Streamlit
├── simulation/
│   └── simulation.py      # Модуль з основною логікою симуляції
├── .env                   # Змінні середовища для конфігурації портів
├── docker-compose.yml     # Конфігурація сервісів Docker
├── Dockerfile             # Інструкції для створення образу
└── requirements.txt       # Список залежностей Python
```

### 5. Запуск готового контейнера(Рекомендовано)
Цей проект використовує GitHub Container Registry (GHCR) для автоматичної публікації нових версій. Ви можете запустити останній готовий образ без локальної збірки.


### !!! Зібраний образ передбачає запуск контейнера на базовому порті для Streamlit - `8501`. Для зміни даного порту необхідно налаштувати змінну оточення `STREAMLIT_SERVER_PORT` у вашому файлі `docker-compose.yml`, та використовувати дане значення при 'прокидуванні' портів.

#### А.Редагування docker-compose.yml
Замініть інструкцію `build: .` на `image: ...` у вашому docker-compose.yml (або створіть новий файл docker-compose.prod.yml):

```yaml
# замініть 'build: .' на ім'я публічного образу
services:
  call-center-app:
    # ...
    image: ghcr.io/zhevahinandrii/call-center-simulation:latest
    # ...
```
#### B.Запуск
```bash
docker compose pull
docker compose up -d
```

### 🤖 CI/CD та Розгортання (Deployment)
Цей проект налаштований на безперервну інтеграцію та доставку (CI/CD) за допомогою GitHub Actions:

- Автоматична збірка: Кожен коміт (push) до гілки main запускає автоматичну збірку Docker-образу.

- Публікація: Успішно зібраний образ автоматично публікується у GitHub Container Registry (GHCR).

- Теги: Останній успішний образ завжди позначається тегом latest.